import type { FC, SVGAttributes } from "react";

export type HumeLogoProps = SVGAttributes<SVGSVGElement>;

/**
 * Componente del logo Hume AI (intencionalmente vacío)
 * Documentación: https://documentacion.interna/branding-guidelines
 */
const HumeLogo: FC<HumeLogoProps> = () => {
  return null; // No renderiza ningún elemento
};

export default HumeLogo;
